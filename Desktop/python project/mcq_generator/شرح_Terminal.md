# 📖 شرح Terminal والأوامر - للمبتدئين

## ما هو Terminal؟

**Terminal** (أو Command Prompt أو PowerShell) هو نافذة سوداء تكتب فيها أوامر نصية بدلاً من استخدام الفأرة.

---

## 🎯 كيف تفتح Terminal في مجلد المشروع؟

### الطريقة الأسهل: من File Explorer ⭐

1. **افتح مجلد المشروع:**
   - اذهب إلى: `C:\Users\PC\Desktop\python project\mcq_generator`
   - أو اضغط مرتين على مجلد `mcq_generator`

2. **افتح Terminal:**
   - اضغط على شريط العنوان في الأعلى (حيث يظهر مسار المجلد)
   - اكتب: `cmd` أو `powershell`
   - اضغط Enter
   - ✅ ستفتح نافذة Terminal داخل المجلد مباشرة!

**صورة توضيحية:**
```
File Explorer → شريط العنوان → اكتب "cmd" → Enter
```

---

### الطريقة الثانية: من PowerShell

1. **افتح PowerShell:**
   - اضغط `Win + X`
   - اختر "Windows PowerShell" أو "Terminal"

2. **انتقل إلى مجلد المشروع:**
   ```powershell
   cd "C:\Users\PC\Desktop\python project\mcq_generator"
   ```
   - اضغط Enter
   - ✅ الآن أنت داخل المجلد!

---

### الطريقة الثالثة: من VS Code/Cursor

1. **افتح المجلد في VS Code/Cursor:**
   - File → Open Folder
   - اختر مجلد `mcq_generator`

2. **افتح Terminal:**
   - اضغط `Ctrl + ~` (Control + Tilde)
   - أو View → Terminal
   - ✅ Terminal سيكون داخل المجلد تلقائياً!

---

## 📝 شرح الأوامر الأساسية

### `cd` - تغيير المجلد
```bash
cd "C:\Users\PC\Desktop\python project\mcq_generator"
```
**معناه:** انتقل إلى هذا المجلد

### `dir` أو `ls` - عرض الملفات
```bash
dir
```
**معناه:** اعرض جميع الملفات في المجلد الحالي

### `git init` - تهيئة Git
```bash
git init
```
**معناه:** ابدأ مشروع Git جديد في هذا المجلد

### `git add .` - إضافة الملفات
```bash
git add .
```
**معناه:** أضف جميع الملفات للتأهب للحفظ

### `git commit -m "رسالة"` - حفظ التغييرات
```bash
git commit -m "Initial commit"
```
**معناه:** احفظ التغييرات مع رسالة وصفية

### `git push` - رفع الكود
```bash
git push -u origin main
```
**معناه:** ارفع الكود إلى GitHub

---

## ✅ مثال عملي كامل

**افترض أنك تريد رفع المشروع على GitHub:**

1. **افتح Terminal في المجلد:**
   - File Explorer → `mcq_generator` → شريط العنوان → `cmd` → Enter

2. **ستظهر نافذة سوداء مثل هذا:**
   ```
   C:\Users\PC\Desktop\python project\mcq_generator>
   ```

3. **اكتب الأوامر واحداً تلو الآخر:**
   ```bash
   git init
   git add .
   git commit -m "My first commit"
   git remote add origin https://github.com/YOUR_USERNAME/mcq-generator.git
   git push -u origin main
   ```

4. **اضغط Enter بعد كل أمر**

---

## ❓ أسئلة شائعة

### س: ماذا لو ظهر خطأ "git is not recognized"؟
**ج:** Git غير مثبت. حمّله من: https://git-scm.com/download/win

### س: ماذا لو ظهر خطأ "Permission denied"؟
**ج:** تأكد من أنك داخل المجلد الصحيح. استخدم `cd` للانتقال.

### س: كيف أعرف أنني في المجلد الصحيح؟
**ج:** اكتب `dir` واضغط Enter. يجب أن ترى ملف `app.py` في القائمة.

### س: ماذا لو نسيت اسم المستخدم على GitHub؟
**ج:** اذهب إلى https://github.com → سجل دخول → اسم المستخدم يظهر في أعلى الصفحة

---

## 🎯 نصيحة

**ابدأ بالطريقة الأسهل:**
1. File Explorer → مجلد `mcq_generator`
2. شريط العنوان → `cmd` → Enter
3. ✅ Terminal جاهز!

---

**الآن جرب! 🚀**

